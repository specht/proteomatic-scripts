group: Chitinator

title: Compare composition fingerprints

description: >
  This script compares two or more composition fingerprints.

type: processor

input:
  - key: compositionFingerprint
    label: composition fingerprint
    formats: [csv]
    min: 2
